services:
  - type: web
    name: prahari-demo
    runtime: php
    plan: free

    buildCommand: |
      composer install --no-dev --optimize-autoloader
      npm install
      npm run build

    startCommand: php artisan serve --host=0.0.0.0 --port=$PORT
